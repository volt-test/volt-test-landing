# Example environment variables for the VoltTest website
# Copy this file to .env (which is gitignored) and replace the placeholder values.
# NOTE: Create React App only exposes variables prefixed with REACT_APP_

# Cloudflare Turnstile public site key (NOT a secret). Obtain from Cloudflare Dashboard.
REACT_APP_TURNSTILE_SITE_KEY=0x4AAAAxxxxxxxxxx

# PostHog public project API key (from your PostHog project settings). Safe to expose.
REACT_APP_PH_PROJECT_API_KEY=phc_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Toggle analytics collection (set to false to disable PostHog init without removing the key)
REACT_APP_ENABLE_ANALYTICS=true
